# Artifact provenance

Every file in `data/` and how it was produced. Hashes are in `SHA256SUMS`
(current state; stamped via OpenTimestamps as `SHA256SUMS.ots`) and in
`SHA256SUMS.precommit` (the manifest as it stood on 2026-08-07, before BLS
published the 2025 thresholds; its proof `SHA256SUMS.precommit.ots` is a
complete Bitcoin attestation — block 961505, mined 2026-08-07 23:18 UTC).

Implementation repository: PolicyEngine/spm-calculator, branch
`max/bls-2026-threshold-correction` (PR #32), commit
`d2746cfacce072f599ffb1788eb195777c6ca9ca` at the time of this revision.
Abbreviated as "impl" below.

| Artifact | Generator | Inputs / source | Notes |
|---|---|---|---|
| `spm_threshold_200524_corrected.xlsx` | none (source file) | https://www.bls.gov/pir/spm/spm_threshold_200524_corrected.xlsx, retrieved 2026-07-17; SHA-256 `e7931a1f…4904c2` pinned in `check_paper.py` | The corrected 2005–2024 workbook BLS published on 2026-07-17. Byte-identical to the live file on 2026-08-06 and 2026-09-04. |
| `spm_thresholds_current_2026-09-04.xlsx` | none (source file) | https://www.bls.gov/pir/spm/spm_thresholds.xlsx (linked from https://www.bls.gov/pir/spmhome.htm), retrieved 2026-09-04; SHA-256 `95f39fb5…e2bd3b` pinned | BLS's rolling current workbook; sheet still titled "2005-2024" but carries 2025 at full precision with standard errors and tenure shares. |
| `threshold_series.json` | impl `scripts/build_threshold_series.py` | the corrected workbook above; P60-275/277/280/283/287 for the pre-correction series (values transcribed and cross-verified between consecutive reports); the package's own ≤0.3.1 dict for the legacy series | Three provenance-stamped series; the corrected series carries per-cell standard errors and tenure shares. Byte-identical to impl `spm_calculator/data/bls/threshold_series.json`. |
| `bls_cpi_series.json` | impl `scripts/benchmark_bls_replication.py` (its CPI fetch, `fetch_bls_cpi_series`) | BLS public data API v2, series CUUR0000SA0, SAF, SAA, SAH1, SAH2, SEED, annual averages 2005–2025 (2025 = eleven-month mean; October 2025 unpublished), retrieved 2026-07-18 | Six-series cache. At impl commit `d2746cf` this file lived in the gitignored `benchmark_output/`; the tracked package store (`cpi_annual.json`) carried a seventh, mis-mapped series — see paper Reproducibility. Being reconciled on the branch. |
| `replication_results.json` | impl `scripts/benchmark_bls_replication.py` | CE Interview PUMD year bundles 2014–2025 (BLS `https://www.bls.gov/cex/pumd_data.htm`, cached under `~/.cache/spm-calculator/ce-pumd`), the CPI cache above, `threshold_series.json` | Replicated thresholds 2019–2025 under anchor {82, 83} × principal {include, exclude} × annualization variants, with signed deviations vs both reference series. Gitignored in impl at `d2746cf`; pinned here. |
| `nowcast_2025.json` | impl `scripts/compute_nowcast_2025.py` | `replication_results.json`, `bls_cpi_series.json`, corrected 2024 base from `threshold_series.json` | The committed nowcast (amended 2026-07-18; original at tag `v1.0-original-nowcast`). Byte-identical to impl `spm_calculator/data/nowcast/nowcast_2025.json`. |
| `nowcast_rate_impact.json` | `scripts/simulate_nowcast_rate_impact.py` (this repo) | PolicyEngine US microsimulation (policyengine-us, editable install of spm-calculator from the PR #32 branch), default Populace dataset as of 2026-07-18 | Poverty-rate sensitivity: CPI-U-aged vs nowcast 2025 thresholds. The committed file was produced by an earlier version of the script that wrote to a scratch path; the script now takes `--output` and stamps its own metadata. Not byte-regenerable without the same dataset vintage; the dataset identifier is recorded in the file. |
| `bls_2025_thresholds.json` | hand-transcribed by the author from the current workbook (values, SEs, shares) and the 2025 threshold page (growth rates, Chart 3, Chart 4) | https://www.bls.gov/pir/spm/spm_thresholds_2025.htm (last modified 2026-08-24) and the current workbook, retrieved 2026-09-04 | Cross-check: full-precision values round to the page's whole dollars (asserted at build time). |
| `census_wp2026_17_rates.json` | hand-transcribed by the author | Creamer, SEHSD WP 2026-17 (2026-08-19), Tables 3 and 4: https://www2.census.gov/library/working-papers/2026/demo/sehsd-wp2026-17.pdf | Revised-vs-corrected SPM rates 2019–2024 and the corrected 2023→2024 rows. |
| `evaluation_2025.json` | `scripts/evaluate_nowcast_2025.py` (this repo) | `bls_2025_thresholds.json`, `nowcast_2025.json`, `bls_cpi_series.json`, `threshold_series.json`, and `git show v1.0-original-nowcast:data/nowcast_2025.json` | Regenerated by the pre-render guard on every build; any drift fails the build. |

Generated tables (`paper/tables/*.md`) are written only by
`scripts/build_tables.py` and `scripts/evaluate_nowcast_2025.py`, both run
by `scripts/check_paper.py`, which also enforces the table allowlist, the
QMD include allowlist, and full `SHA256SUMS` coverage of `data/`.


## September 8, 2026 amendment and current inputs

The original `SHA256SUMS`, `SHA256SUMS.precommit`, and
`COMMITMENT-MANIFEST.txt` and all three `.ots` proofs remain byte-identical.
`COMMITMENT-AMENDMENT-2026-09-08.txt` corrects the two tag-artifact hashes
without claiming coverage by the old proof. `verify_commitments.py` checks
those original bytes, tag objects, peeled commits, and exact forecast bytes.
It does not contact OpenTimestamps calendars or verify Bitcoin attestations.

`data/current/SHA256SUMS` separately covers these new inputs:

| Artifact | Source and scope |
|---|---|
| `spm-release.json` | Exact portable `spm-2026-09-08.json` from the spm-calculator 0.5.0 rebuild. Revised national 2005–2025 published thresholds, source fingerprints, separately dated 2024 housing shares, and pinned geographic rent indices. The release's conservative available-on date is September 8, not a reconstructed historical publication calendar. |
| `ce_replication_2019_2025.json` | `scripts/replicate_current_ce.py` in the rebuild. Seven overlapping CE windows, explicit minor-only and tenure approximations, raw source bundle hashes, CPI cache hash, exact executed source hashes, and six-year retrospective projections. No prospective validation or new commitment. |
| `provenance.json` | Paper linkage to the release content digest and CE artifact/source hashes. Pins spm-calculator 0.5.0 development commit `0d7fa0d77b0a88064ab7b9fe70557309b5f7901f`. The retained `ce_starting_git_head` identifies the starting commit for this replay, before its source and artifact update was committed. The `ce_code_sha256` map identifies the executed code, whose bytes match the pinned development commit. |

`build_current_tables.py` reads only these inputs. It cross-checks the
published comparison series against the shared release, re-derives
replication and sensitivity percentages and projection errors, and
produces the four `current_*.md` tables. The pre-render guard checks both
manifests before parsing inputs, generates all tables and evaluation JSON
in scratch space, and compares bytes without mutating source files. These
checks establish reproducibility of the recorded calculations, not exact
parity with the BLS production procedure or historical information sets.
